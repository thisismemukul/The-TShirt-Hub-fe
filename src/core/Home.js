import React from 'react';
import homeImg from '../images/home.png';
import Base from './Base';
const Home = () => {
   return (
      <Base Img={homeImg} title="Better when it's on you." description="Anyone can beat you but no one can beat your outfit as long as you wear The T-Shirt Hub's outfits." button="Get one for me">
      </Base>
   );
};

export default Home;
